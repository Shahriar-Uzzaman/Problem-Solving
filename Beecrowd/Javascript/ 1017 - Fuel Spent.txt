var input = require('fs').readFileSync('/dev/stdin', 'utf8');

const inputs = input.trim().split('\n');

const time = parseInt(inputs[0]);
const avgSpeed = parseInt(inputs[1]);

const fuel = (time * avgSpeed) / 12;

console.log(fuel.toFixed(3));